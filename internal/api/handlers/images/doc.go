// Package images provides the /v1/images HTTP API endpoint, exposing the
// current image identity (name, local ID, registry manifest digest) and
// container count for each image Watchtower tracks.
package images
