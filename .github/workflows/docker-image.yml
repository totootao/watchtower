name: Build and push Docker image

# Builds the Watchtower web-dashboard image and pushes it to Docker Hub.
# The push only happens on pushes to the default branch / web-ui branch and on
# tags; pull requests get a build-only run so forks can validate without secrets.

on:
  push:
    branches: [main, web-ui]
    tags: ["v*"]
  pull_request:
    branches: [main, web-ui]
  workflow_dispatch:

env:
  IMAGE_NAME: totootao/watchtower

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    # Map the Docker Hub secrets to job-level env vars so they can be referenced
    # from `if:` conditions (the `secrets` context is NOT allowed inside `if`).
    env:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        # Only log in when the Docker Hub secrets are configured. Without them the
        # build still succeeds (build-only), and the push is skipped automatically.
        if: github.event_name != 'pull_request' && env.DOCKERHUB_USERNAME != ''
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          # Push only when the Docker Hub secrets are present; otherwise build-only.
          push: ${{ github.event_name != 'pull_request' && env.DOCKERHUB_USERNAME != '' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ github.ref_type == 'tag' && github.ref_name || github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
